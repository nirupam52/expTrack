services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    volumes:
      - expense-data:/data

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend

volumes:
  expense-data:
